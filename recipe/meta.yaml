{% set name = "fastapi-mvc" %}
{% set version = "0.13.0" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/fastapi-mvc-{{ version }}.tar.gz
  sha256: 7664c8bd5291868121ccf100c8f521ca301c0688e0251e8092f0e5877747ed8a

build:
  number: 0
  noarch: python
  entry_points:
    - fastapi-mvc = fastapi_mvc.cli.cli:cli
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - poetry-core >=1.0.0
    - python >=3.7
  run:
    - click ~=8.1.3
    - cookiecutter ~=1.7.3
    - python >=3.7

test:
  imports:
    - fastapi_mvc
    - fastapi_mvc.cli
    - fastapi_mvc.commands
    - fastapi_mvc.generators
    - fastapi_mvc.generators.controller
    - fastapi_mvc.generators.generator
    - fastapi_mvc.parsers
    - fastapi_mvc.utils
  commands:
    - pip check
    - fastapi-mvc --help
  requires:
    - pip

about:
  home: https://github.com/rszamszur/fastapi-mvc
  summary: Generate and manage FastAPI projects
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thewchan
